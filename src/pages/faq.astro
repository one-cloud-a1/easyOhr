---
import BaseLayout from '../layouts/BaseLayout.astro'
import FAQAccordion from '../components/FAQAccordion.tsx'
import faqData from '../data/faq.json'
import company from '../data/company.json'
import { url } from '../lib/links'
---
<BaseLayout title="Häufige Fragen" description="Antworten auf die häufigsten Fragen zu Bestellung, Testphase, Zahlung und Rückgabe bei easyOhr.">
  <section class="page-header">
    <div class="container">
      <h1>Häufige Fragen</h1>
      <p>Alles was Sie über easyOhr wissen müssen</p>
    </div>
  </section>

  <section class="section" style="padding-top: 2rem;">
    <div class="container faq-container">
      <FAQAccordion client:load items={faqData} />
      <div class="faq-contact">
        <h3>Ihre Frage nicht dabei?</h3>
        <p>Schreiben Sie uns — wir antworten in der Regel noch am selben Tag.</p>
        <a href={`mailto:${company.email}`} class="btn btn-primary">E-Mail schreiben</a>
        <a href={url('/anfrage/')} class="btn btn-outline">Angebot anfordern</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-header {
    background: var(--color-secondary);
    padding: 3rem 0;
    text-align: center;
  }

  .page-header p {
    color: var(--color-text-light);
    font-size: 1.1rem;
    margin-top: 0.75rem;
  }

  .faq-container {
    max-width: 720px;
  }

  .faq-contact {
    text-align: center;
    padding: 3rem 2rem;
    background: var(--color-secondary);
    border-radius: var(--radius-lg);
    margin-top: 3rem;
  }

  .faq-contact h3 {
    margin-bottom: 0.5rem;
  }

  .faq-contact p {
    color: var(--color-text-light);
    margin-bottom: 1.5rem;
  }

  .faq-contact .btn {
    margin: 0 0.5rem;
  }
</style>

<style is:global>
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .faq-item {
    border: 1px solid var(--color-border-light);
    border-radius: var(--radius-md);
    overflow: hidden;
    transition: border-color 0.2s;
  }

  .faq-item.faq-open {
    border-color: var(--color-primary);
  }

  .faq-question {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.125rem 1.25rem;
    background: none;
    border: none;
    font-size: 1rem;
    font-weight: 600;
    text-align: left;
    color: var(--color-text);
    cursor: pointer;
    gap: 1rem;
  }

  .faq-question:hover {
    color: var(--color-primary);
  }

  .faq-answer {
    padding: 0 1.25rem 1.25rem;
  }

  .faq-answer p {
    color: var(--color-text-light);
    line-height: 1.7;
  }
</style>
